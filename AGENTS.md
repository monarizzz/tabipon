# AGENTS.md

このリポジトリで作業するコーディングエージェント共通の指示。
指示はこのファイルに集約する。`CLAUDE.md` は本ファイルを読み込むだけで、内容を持たない。

## リポジトリ構成

このリポジトリはフロントエンドとバックエンドをモノレポで管理する。

```text
/
├── frontend/   # React Native / Expo アプリ (TypeScript)
├── backend/    # Python / FastAPI サーバー（#98 で廃止予定。下記参照）
├── docs/       # 仕様書・アーキテクチャドキュメント
└── design/     # デザインファイル・スタイルガイド
```

- プロジェクトの仕様書は `docs/REQUIREMENTS.md` に記載されている
- frontend 固有の指示は `frontend/AGENTS.md` を参照すること

## バックエンド廃止の方針（#98）

`backend/`（画像生成・永続化・認証）は端末ローカル実装への移行に伴い廃止予定の [Epic #98](https://github.com/monarizzz/tabipon/issues/98) が進行中。
スタンプの保存先は Supabase から端末ローカル（expo-sqlite / expo-file-system）へ移す方針が決まっている。

- `backend/` に手を入れる前に #98 と配下のフェーズ Issue（#99〜#103）の状況を確認すること
- Supabase 側のストレージ改善など、廃止予定の資産に対する新規提案は、まず要否を確認してから着手する

## 基本ルール

- 必ず日本語で応答すること
- Autoで実装する場合は、Commitまで実行しPushはユーザーが明示的に示した場合のみとする
- force push（`git push --force` / `--force-with-lease` 等）は行わない。実行するのは、ユーザーが明示的に指示した例外的な場合のみとする

## ブランチ

- ベースブランチは `main`。作業ブランチはここから切り、PR も `main` 宛てに作成する

## コミット

### 粒度

**1 コミット = 1 コンポーネント / 1 論点。**まとめて 1 コミットにしない。

- 新規コンポーネントは「実装 + `.stories.tsx` + 対応表の該当行」の 3 点で 1 コミット。
  ストーリーを別コミットにすると片方だけ revert できてしまう
- **既存コンポーネントへの変更は独立したコミットにする。**新規実装と同じコミットに埋めない。
  既存への波及を疑われる箇所ほど、単独で見えている必要がある
- ルーティング（`frontend/app/` 配下）の結線はコンポーネント実装と分ける。レビューの観点が違う
- 順序は依存の下から上へ。**各コミットの時点で型チェックが通る**こと（frontend のコマンドは `frontend/AGENTS.md` を参照）

### メッセージ

**そのコミット単体で、何をどう変えたかが分かること。**

件名:

- 何を・どこに・どうしたかを書く。`実装` `追加` だけで終わらせない
- 既存改修なら「〜に〜を追加」と、新規と書き分ける

本文（新規コンポーネント・判断を伴う変更には必須）:

- 実装したバリアント・状態の数
- **推測で埋めた箇所、自分で決めたロジック。**ここが最も重要。
  書き残さないと、後から「なぜこの形か」を追えなくなる
- 既存コンポーネントへ影響が無いなら、無いと明記する

## PRとIssueの紐づけ

**PR 本文の「## 関連 Issue」節を必ず埋める。**空欄・テンプレのコメントのまま
提出しない。紐づけが無いと、Issue 側から対応する PR を辿れなくなる。

- そのPRで閉じる Issue: `Close #123`
- 関連するだけの Issue: `Refs #123`
- 対応する Issue が無い場合は「なし」と明記する

作業を始める前に、対応する Issue があるかを確認すること。
引数なしの `gh issue list` は open のみ・既定 30 件しか出さないため、
これだけで「無い」と判断すると重複 Issue を作ってしまう。
キーワードと取得件数、閉じた Issue まで含めて探す。

```bash
gh issue list --state all --limit 200 --search '<キーワード>'
```

見つからず、かつ単発の修正でないなら Issue を先に立てる。

`Close` キーワードによる自動クローズは、PR の宛先がデフォルトブランチ（`main`）のときに効く。

## PRのスクリーンショット

UI に変更がある PR では、スクリーンショットを**必ず PR 本文の
「## スクリーンショット」節に埋め込む**こと。コメントに貼るだけで終わらせない。

`gh` は本文へ画像を直接添付できないため、次の 3 段構えで行う。

1. `gh pr comment <PR> --attach '<画像>#<代替テキスト>'` で画像をアップロードする
2. 返ってきた画像 URL を `gh pr edit <PR> --body-file <本文ファイル>` で本文の該当節に差し込む
3. `gh api -X DELETE repos/<owner>/<repo>/issues/comments/<コメントID>` で
   1 で作ったコメントを削除する

1 のコメントは URL を得るための手段でしかないので、本文へ差し込んだら消す。
画像アセットはコメントの寿命と独立しており、コメントを削除しても本文の画像は表示され続ける。

ただしアップロードの経路がコメントしか無いため、投稿した時点で Watch している
メンバーへ通知が 1 通飛ぶ。コメントを消しても送信済みの通知は取り消せない。
これは回避できないので許容する。

`--attach` は新しめの `gh` にしか無い。`gh pr comment --help` に `--attach` が
出ない場合は、GitHub のブラウザ上で PR 本文の編集画面に画像をドラッグ&ドロップして貼る。
その場合も貼る先は本文であり、コメントではない。

- 変更前後が分かるように並べる
- 画面全体のスクショを貼る場合は、変更箇所を枠線などで囲む
- UI 変更が無い場合は節を空のままにせず「UI 変更なし」と明記する

## worktree

作業用に worktree を作った場合、**その作業が終わった時点で必ず削除する**こと
（別の作業を引き続き行う場合や、ユーザーから残すよう指示された場合は除く）。

```bash
git worktree remove <パス>   # 空になった親ディレクトリも消す
git worktree list            # 消えたことを確認
```

- 消すのは worktree だけで、ブランチは残す
- 削除前に `git status` で未コミットの変更が無いことを確認する
- 放置するとリポジトリ全体の複製がツリー内に残り、検索やレビューのノイズになる

## コードレビューの言語

- レビューコメントは**必ず日本語で記述すること**
- 指摘のタイトル・本文・要約(サマリ)のすべてを日本語にすること
- コード片・識別子・ファイルパス・エラーメッセージの引用は原文のまま残してよい

## レビューの観点

- バグや不具合につながる実装
- パフォーマンス上の問題
- セキュリティ上の懸念
- テストの不足
